# CRM ｜ いまどこまで

**2026年9月19日　ここでいったん止めています。**
次に再開するときは、この書類を最初に読んでください。

※ 合言葉・Apps Script のURL・オーナー様の情報は、この書類にも
　 このリポジトリにも一切書いていません（公開されているためです）。

---

## 動いているもの

| | |
|---|---|
| 画面 | `crm.html` 1枚。ここに全部入っています |
| 見た目 | base44 でお作りになった画面（`Dashboard.jsx` / `Layout.jsx` / `InquiryDetail.jsx`）のとおり |
| クラウド | Apps Script（`crmApi.gs` ほか）。JSONP で読み書きします |
| 合言葉 | 入れてあります。`REQUIRE_KEY = true` にして再デプロイ済み |

### 画面の作り

```
上の帯（Layout）      濃紺・56px。案件／ゴミ箱／分析、検索、送り先
  ↓
ヒーロー              対応中件数・3日以上たっている
エリアのカード         福山／倉敷／岡山（＋あれば未分類）
しぼり込み            エリア／カテゴリ／物件／クリア／N件表示
かんばん3列           未返信・返事待ち・完了
  ↓ カードを押す
詳細（InquiryDetail） 濃紺の見出し／対応ジャーニー6段／
                      問い合わせ内容・メール返信・AI・添付・履歴／管理情報
```

### 支社を決める順番（ここが要）

```
① サーバーが area を返していれば、それが正      ← ★まだ返っていません
② 人が画面で決めたもの（「どの支社？」ボタン）
③ 物件名の表 BLDG_AREA（福山153件）
     倉敷市老松町・玉島乙島 → 福山（本社）の例外もここ
④ HPのフォームで選ばれた「お住まいの地域」      ← ★まだ届いていません
⑤ 本文に住所が書かれていれば、市名で
     岡山市→岡山／倉敷市・総社市→倉敷／福山市→福山
     「岡山県」だけで市名が無いものは決めつけない
```

---

## 止まっている理由（次にやること）

**クラウド側（Apps Script）が、台帳に「支社」を書いていません。**

HP のフォームに「お住まいの地域」が足され、通知メールには
`■お住まいの地域` の段が入るようになりました。しかし Apps Script が
その段を台帳に入れていないため、画面まで届いていません。

### 次の一手（順番）

1. **`crmArea.gs` を新規作成して貼る**
   　→ お渡し済み。貼って `crmAreaTest_ミテミル` を実行し、
   　　 実行ログが「※ ぜんぶ合っています」になれば正しく貼れています。
   　　 このファイルだけでは何も変わりません（まだ誰も呼んでいません）。
2. **`crmList.gs` を見て、呼ぶ1行を足す**
   　→ 現物を見ないと書けないため、ここで止まっています。
   　　 やることは2つだけです。
   　　 　・台帳に「支社」の列を1つ増やす
   　　 　・一覧を返すところで `area: crmAreaOf_(本文)` を添える
   　→ 画面側は `area` を最優先で読む作りなので、
   　　 届いた瞬間から自動で支社に分かれます。
3. デプロイは「デプロイを管理 → 鉛筆 → 新バージョン」

### それまでの逃げ道

画面の「どの支社？ 福山 倉敷 岡山」を押せば分けられます。
一度押せば同じ物件はまとめて分かれます。
押した内容は「覚えた振り分け」から写せます。
**ただし覚える先はその端末だけで、他の方の画面には出ません。**

---

## 分かっていて、直していないこと

- **「その他」を選ばれたものの扱いが未定。**
  いまは未分類にして人に決めていただいています。
  「その他＝福山本社が受ける」のように決まれば、自動にできます。
- **岡山支社・倉敷支社の物件名の表が空。**
  手元の PIVOT3 のデータに住所が無く、岡山市か倉敷市かを
  決められないためです。推測では入れていません。
  ※ 地域の選択が必須になるので、この表は控えの位置づけです。
- **「メルヴェイユ」など、表に無い物件があります。**
  表のもとにした控えが 2026/8/26 時点のもので、
  そのあと増えた物件は入っていません。

---

## 検査

`crm.html` を headless Chromium で動かす検査を置いています
（この入れものの外、作業用の場所にあります）。

| 検査 | 件数 | 見ているもの |
|---|---|---|
| `tcrmui.cjs` | 59 | 画面の組み立て・支社の見分け・しぼり込み・詳細・色 |
| `tarea.cjs` | 29 | 支社の見分けだけを集中的に |
| `tgomi.cjs` | 22 | ゴミ箱と、支社を手で決めるところ |
| `tgate.cjs` | 24 | 合言葉 |
| `tsum.cjs` | 8 | カードの見出し（実物のメール全文で） |
| `_tname.cjs` | 5 | お名前の拾い方 |

**2026/9/19 時点で、すべて合格しています。**
